
<% include header %>
  <% if (user) { %>

  <div class="container top-buffer">
    <div class="row">
      <%- include categories %>

       <main class="col-sm-9 offset-sm-3 col-md-9 offset-md-2 pt-3">
		<div class="row">
				<div class="col-sm-10">
					<p class="header"><%= (notes[0].title) %></p>
				</div>
				<div class="col-sm-2">
					<a href="/modify_note/<%= notes[0].id %>" type="button" class="btn btn-xs btn-success"><span class="glyphicon glyphicon-plus">Редактировать</a>
				</div>
		</div>
			<div class="result-html full-height"><hr>
			<%- (notes[0].description) %>
			</div>
		</main>


    </div>
  </div>
<% } else { %>
  <% include checkin%>
<% } %>
<%- include footer %>
